import styles from "./List.module.css";
import Clipboard from "../assets/Clipboard.svg";
import { useState } from "react";

export function List() {
    const [tasks, setComments] = useState(["Legal!"]);

  return (
    <div>
      
    </div>
  );
}
